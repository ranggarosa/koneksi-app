interface:
  display_name: "Koneksi App"
  short_description: "Repo-specific patterns and workflows for koneksi-app"
  default_prompt: "Use the koneksi-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true